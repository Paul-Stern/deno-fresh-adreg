import { FreshContext } from "$fresh/server.ts";
import { PageProps } from "$fresh/server.ts";

// export const handler = (_req: Request, _ctx: FreshContext, props: PageProps): Response => {
//    return <div>Dota 2 match id is: {props.params.name}</div>;
// }
